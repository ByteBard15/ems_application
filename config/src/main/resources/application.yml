server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: ${GIT_REPO_LINK}
          default-label: master
          search-paths: .